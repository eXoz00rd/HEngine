using Xunit;

namespace HEngine.Rendering.Tests.Smoke;

public class SmokeTests
{
    [Fact(DisplayName = "Rendering tests smoke should run")]
    public void Smoke_Should_Run()
    {
        Assert.True(true);
    }
}
